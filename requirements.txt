# ===================================================
# PentestAI 依赖清单
# Python >= 3.11 推荐
# 安装：pip install -r requirements.txt
# ===================================================

# ----- Web 框架 -----
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ----- LangGraph / LangChain（Agent 编排）-----
langgraph==0.2.28
langchain==0.3.1
langchain-core==0.3.6
langchain-openai==0.2.1          # DeepSeek/GPT 共用（兼容 OpenAI SDK）
langgraph-checkpoint-sqlite
langgraph-checkpoint-postgres

# ----- OpenAI SDK（DeepSeek 兼容接口）-----
openai==1.51.0

# ----- Metasploit RPC 客户端 -----
pymetasploit3==1.0.3

# ----- 数据验证 / 序列化 -----
pydantic==2.9.2
pydantic-settings==2.5.2

# ----- 模板引擎（报告生成）-----
jinja2==3.1.4

# ----- 数据库（可选，生产建议启用）-----
sqlalchemy==2.0.35
asyncpg==0.29.0                  # PostgreSQL 异步驱动
aiosqlite==0.20.0                # SQLite（开发调试用）

# ----- Redis（任务队列 / 状态缓存）-----
redis==5.1.1
aioredis==2.0.1

# ----- HTTP 客户端 -----
httpx==0.27.2
aiohttp==3.10.8

# ----- XML 解析（Nmap 输出）-----
lxml==5.3.0

# ----- 工具函数 -----
python-dotenv==1.0.1             # .env 文件加载
tenacity>=8.1.0,<9.0.0           # 重试装饰器（langchain-core 要求 <9.0.0）
structlog==24.4.0                # 结构化日志
rich==13.8.1                     # 终端彩色输出（调试）
pyyaml

# ----- 测试 -----
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0
httpx                            # TestClient 依赖（已在上方列出）
minio==7.2.8                     # MinIO 对象存储

# ----- 系统资源监控 & 管理员终端 -----
psutil==6.0.0                    # 宿主机 CPU/内存/磁盘采集（GET /admin/system-metrics）
docker==7.1.0                    # Docker 容器资源 & 生命周期控制（Admin Dashboard + /admin/docker/*）
asyncssh==2.17.0                 # 管理员 SSH WebSocket 代理（/admin/terminal）